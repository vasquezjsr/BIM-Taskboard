<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Matches SpoolingManagerPane.xaml / FindAndFilterPane chrome -->
    <SolidColorBrush x:Key="SsSavantChromeBackground" Color="#FF2B2B2B"/>
    <SolidColorBrush x:Key="SsSavantBorderOuter" Color="#FF555555"/>
    <SolidColorBrush x:Key="SsSavantListWellBackground" Color="#FF333333"/>
    <SolidColorBrush x:Key="SsSavantListBorder" Color="#FF666666"/>
    <SolidColorBrush x:Key="SsSavantInputBackground" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="SsSavantForegroundPrimary" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="SsSavantForegroundMuted" Color="#FFCFCFCF"/>
    <SolidColorBrush x:Key="SsSavantForegroundTitle" Color="#FFEAEAEA"/>
    <SolidColorBrush x:Key="SsSavantHighlightBrush" Color="#FF505050"/>
    <SolidColorBrush x:Key="SsSavantPressedBrush" Color="#FF383838"/>
    <SolidColorBrush x:Key="SsSavantShortcutTileBackground" Color="#FF505050"/>
    <SolidColorBrush x:Key="VgOnDemandChromeBackground" Color="#FF2B2B2B"/>

    <!-- ComboBox drop-down popups still consult SystemColors; map them to the dark palette so list text stays readable. -->
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF505050"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFF0F0F0"/>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource SsSavantChromeBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
    </Style>

    <Style TargetType="UserControl">
        <Setter Property="Background" Value="{DynamicResource SsSavantChromeBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
    </Style>

    <!-- Custom template so the glyph stays high-contrast on dark chrome; default Aero uses a near-black check on grey. -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="8,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <BulletDecorator Background="Transparent" SnapsToDevicePixels="True">
                        <BulletDecorator.Bullet>
                            <Grid Width="15" Height="15" VerticalAlignment="Center">
                                <Border x:Name="CheckBorder"
                                        Width="15" Height="15"
                                        CornerRadius="2"
                                        BorderThickness="1"
                                        BorderBrush="{DynamicResource SsSavantListBorder}"
                                        Background="Transparent"/>
                                <Path x:Name="CheckMark"
                                      Data="M 2.5 7.5 L 5.5 10.5 L 12 3.5"
                                      Stroke="{DynamicResource SsSavantForegroundPrimary}"
                                      StrokeThickness="1.75"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      Visibility="Collapsed"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      Margin="1"/>
                            </Grid>
                        </BulletDecorator.Bullet>
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"
                                          Focusable="False"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </BulletDecorator>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="HasContent" Value="False">
                <Setter Property="Padding" Value="0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="CaretBrush" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="ChromeBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <!-- Default Aero template ignores ComboBox.Background on the collapsed slot; replace template so match TextBox chrome. -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid SnapsToDevicePixels="True"
                          HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="22"/>
                        </Grid.ColumnDefinitions>
                        <ToggleButton x:Name="PART_Button"
                                      Grid.ColumnSpan="2"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch"
                                      Focusable="False"
                                      ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                            BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                            BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                            CornerRadius="8"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            SnapsToDevicePixels="True"/>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Grid.Column="0"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          IsHitTestVisible="False"
                                          Focusable="False"
                                          RecognizesAccessKey="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                        <!-- IsEditable=True: selection is drawn here; must match dark input chrome (see triggers). -->
                        <TextBox x:Name="PART_EditableTextBox"
                                 Grid.Column="0"
                                 Margin="{TemplateBinding Padding}"
                                 HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                 VerticalContentAlignment="Center"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Focusable="True"
                                 Visibility="Collapsed"
                                 Background="Transparent"
                                 Foreground="{TemplateBinding Foreground}"
                                 BorderThickness="0"
                                 Padding="0"
                                 CaretBrush="{DynamicResource SsSavantForegroundPrimary}"/>
                        <Border Grid.Column="1"
                                Background="Transparent"
                                IsHitTestVisible="False">
                            <Path Fill="{DynamicResource SsSavantForegroundPrimary}"
                                  Stretch="Uniform"
                                  Width="10"
                                  Height="6"
                                  Data="M 0 1 L 5 6 L 10 1 Z"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Border>
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               PlacementTarget="{Binding ElementName=PART_Button}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               MinWidth="{Binding ActualWidth, ElementName=PART_Button}"
                               Width="{Binding ActualWidth, ElementName=PART_Button}">
                            <Border Background="{DynamicResource SsSavantInputBackground}"
                                    BorderBrush="{DynamicResource SsSavantListBorder}"
                                    BorderThickness="1"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    HorizontalAlignment="Stretch">
                                <ScrollViewer Margin="0"
                                              SnapsToDevicePixels="True"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"
                                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_ContentPresenter" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Dropdown layer uses its own chrome; without this, items pick up light popup backgrounds with inherited pale foreground. -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource SsSavantPressedBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource SsSavantListWellBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{DynamicResource SsSavantListWellBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="RowBackground" Value="{DynamicResource SsSavantListWellBackground}"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource SsSavantBorderOuter}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource SsSavantBorderOuter}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundTitle}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantBorderOuter}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Padding" Value="6,4"/>
    </Style>

    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource SsSavantBorderOuter}"/>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="1"
                                              BlurRadius="5"
                                              Opacity="0.22"
                                              Direction="270"/>
                        </Border.Effect>
                        <StackPanel IsItemsHost="True"
                                    KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="MenuItemBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="MenuItemBorder" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TreeView">
        <Setter Property="Background" Value="{DynamicResource SsSavantListWellBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="2,1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="19"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <ToggleButton x:Name="Expander"
                                      ClickMode="Press"
                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      Width="16"
                                      Height="16"
                                      Margin="1,0,2,0"
                                      VerticalAlignment="Center">
                            <Path x:Name="ExpanderGlyph"
                                  Data="M 0 0 L 4 4 L 0 8 Z"
                                  Fill="{DynamicResource SsSavantForegroundPrimary}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </ToggleButton>
                        <Border x:Name="Bd"
                                Grid.Column="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="PART_Header"
                                              ContentSource="Header"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              TextElement.Foreground="{DynamicResource SsSavantForegroundPrimary}"/>
                        </Border>
                        <ItemsPresenter x:Name="ItemsHost"
                                        Grid.Row="1"
                                        Grid.Column="1"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VgRoundedTabItem" TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundMuted}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            Margin="4,0,4,0"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="HeaderPresenter"
                                          ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource SsSavantListWellBackground}"/>
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
                            <Setter TargetName="TabBorder" Property="BorderThickness" Value="1,1,1,0"/>
                            <Setter TargetName="TabBorder" Property="CornerRadius" Value="8,8,0,0"/>
                            <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Panel.ZIndex" Value="20"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VgRoundedTabControl" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource VgRoundedTabItem}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                Background="{DynamicResource SsSavantInputBackground}"
                                CornerRadius="10,10,0,0"
                                BorderBrush="{DynamicResource SsSavantListBorder}"
                                BorderThickness="1,1,1,0"
                                ClipToBounds="False"
                                Padding="8,6,8,0">
                            <TabPanel IsItemsHost="True"/>
                        </Border>
                        <Border Grid.Row="1"
                                Margin="0,-1,0,0"
                                Background="{DynamicResource SsSavantListWellBackground}"
                                CornerRadius="0,0,10,10"
                                BorderBrush="{DynamicResource SsSavantListBorder}"
                                BorderThickness="1"
                                Padding="8">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ShadowBorder"
                            CornerRadius="8"
                            Margin="0,1,0,0"
                            Background="#44000000"
                            SnapsToDevicePixels="True">
                        <Border x:Name="ChromeBorder"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextOptions.TextFormattingMode="Display"
                                              TextOptions.TextRenderingMode="ClearType"/>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ChromeBorder" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ChromeBorder" Property="Background" Value="{DynamicResource SsSavantPressedBrush}"/>
                            <Setter TargetName="ShadowBorder" Property="Margin" Value="0,2,0,0"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VgSquareButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ShadowBorder"
                            CornerRadius="5"
                            Margin="0,1,0,0"
                            Background="#44000000"
                            SnapsToDevicePixels="True">
                        <Border x:Name="ChromeBorder"
                                CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextOptions.TextFormattingMode="Display"
                                              TextOptions.TextRenderingMode="ClearType"/>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ChromeBorder" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ChromeBorder" Property="Background" Value="{DynamicResource SsSavantPressedBrush}"/>
                            <Setter TargetName="ShadowBorder" Property="Margin" Value="0,2,0,0"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
