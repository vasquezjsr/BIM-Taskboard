<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Revit Options–style dialog defaults (light). PaneThemeApplier overrides from Revit UI at runtime. -->
    <SolidColorBrush x:Key="SsSavantChromeBackground" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="SsSavantBorderOuter" Color="#FFC8C8C8"/>
    <SolidColorBrush x:Key="SsSavantListWellBackground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="SsSavantListBorder" Color="#FFB0B0B0"/>
    <SolidColorBrush x:Key="SsSavantInputBackground" Color="#FFECECEC"/>
    <SolidColorBrush x:Key="SsSavantForegroundPrimary" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="SsSavantForegroundMuted" Color="#FF5A5A5A"/>
    <SolidColorBrush x:Key="SsSavantForegroundTitle" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="SsSavantHighlightBrush" Color="#FFE4E4E4"/>
    <SolidColorBrush x:Key="SsSavantPressedBrush" Color="#FFD0D0D0"/>
    <SolidColorBrush x:Key="SsSavantShortcutTileBackground" Color="#FFECECEC"/>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource SsSavantChromeBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
    </Style>

    <Style TargetType="GroupBox">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantBorderOuter}"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="Padding" Value="8,4,8,8"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="MinWidth" Value="75"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="6,0,0,0"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SsSavantPressedBrush}"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#FF4DA3FF"/>
                            <Setter TargetName="Bd" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#FF4DA3FF"
                                                      BlurRadius="10"
                                                      ShadowDepth="0"
                                                      Opacity="0.70"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Padding" Value="8,0,8,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CaretBrush" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Padding="{TemplateBinding Padding}"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch"
                                      HorizontalContentAlignment="Left"
                                      VerticalContentAlignment="Center"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Toggle button (box + arrow) used by the themed ComboBox template. -->
    <ControlTemplate x:Key="SsSavantComboToggleButton" TargetType="ToggleButton">
        <Border x:Name="Bd"
                Background="{DynamicResource SsSavantInputBackground}"
                BorderBrush="{DynamicResource SsSavantListBorder}"
                BorderThickness="1"
                CornerRadius="8"
                SnapsToDevicePixels="True">
            <Path x:Name="Arrow"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Center"
                  Margin="0,0,8,0"
                  Data="M0,0 L8,0 L4,4 Z"
                  Fill="{DynamicResource SsSavantForegroundMuted}"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource SsSavantForegroundMuted}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="ComboBox">
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Padding" Value="8,0,26,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Template="{StaticResource SsSavantComboToggleButton}"
                                      Focusable="False"
                                      ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          TextElement.Foreground="{DynamicResource SsSavantForegroundPrimary}"/>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Margin="4,1,24,1"
                                 VerticalContentAlignment="Center"
                                 Background="Transparent"
                                 Foreground="{DynamicResource SsSavantForegroundPrimary}"
                                 CaretBrush="{DynamicResource SsSavantForegroundPrimary}"
                                 BorderThickness="0"
                                 Padding="0"
                                 Visibility="Hidden"
                                 IsReadOnly="{TemplateBinding IsReadOnly}"/>
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Border x:Name="DropDownBorder"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Margin="0,2,0,0"
                                    Background="{DynamicResource SsSavantInputBackground}"
                                    BorderBrush="{DynamicResource SsSavantListBorder}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer>
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundMuted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True"
                            TextElement.Foreground="{DynamicResource SsSavantForegroundPrimary}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VgRoundedTabItem" TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundMuted}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            Margin="4,0,4,0"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="HeaderPresenter"
                                          ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource SsSavantListWellBackground}"/>
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
                            <Setter TargetName="TabBorder" Property="BorderThickness" Value="1,1,1,0"/>
                            <Setter TargetName="TabBorder" Property="CornerRadius" Value="8,8,0,0"/>
                            <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Panel.ZIndex" Value="20"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource SsSavantHighlightBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VgRoundedTabControl" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource VgRoundedTabItem}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                Background="{DynamicResource SsSavantInputBackground}"
                                CornerRadius="10,10,0,0"
                                BorderBrush="{DynamicResource SsSavantListBorder}"
                                BorderThickness="1,1,1,0"
                                ClipToBounds="False"
                                Padding="8,6,8,0">
                            <TabPanel IsItemsHost="True"/>
                        </Border>
                        <Border Grid.Row="1"
                                Margin="0,-1,0,0"
                                Background="{DynamicResource SsSavantListWellBackground}"
                                CornerRadius="0,0,10,10"
                                BorderBrush="{DynamicResource SsSavantListBorder}"
                                BorderThickness="1"
                                Padding="8">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource SsSavantListWellBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{DynamicResource SsSavantListWellBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="RowBackground" Value="{DynamicResource SsSavantListWellBackground}"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource SsSavantBorderOuter}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource SsSavantBorderOuter}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource SsSavantInputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantListBorder}"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="BorderBrush" Value="{DynamicResource SsSavantBorderOuter}"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Padding" Value="4,2"/>
    </Style>

    <Style x:Key="SsSavantDialogTitleText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundPrimary}"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="SsSavantDialogHintText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource SsSavantForegroundMuted}"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
</ResourceDictionary>
